#!/usr/bin/make -f

%:
	dh $@ --fail-missing

override_dh_auto_build:
	$(MAKE) vvfst SM_BUILDDATE="$(shell dpkg-parsechangelog | sed -n -e 's/^Date: //p')"
	
override_dh_auto_install:
	$(MAKE) vvfst-install DESTDIR=$(CURDIR)/debian/voikko-fi/usr/lib/voikko

